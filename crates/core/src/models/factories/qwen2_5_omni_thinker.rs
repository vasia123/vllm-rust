//! AUTO-GENERATED Phase 2 factory for `Qwen2_5OmniThinkerForConditionalGeneration`.
//!
//! Generated by `scripts/generate_factories.py` from the legacy match-arm
//! dispatch in `crates/core/src/models/mod.rs`. Bodies are lifted
//! verbatim — re-running the generator overwrites this file.
//!
//! Capabilities: build

#![allow(unused_imports, unused_variables)]

use std::any::Any;

use candle_nn::VarBuilder;

use crate::config::ModelConfig;
use crate::engine::ModelForward;

use super::super::factory::{ArchFactory, ArchInfo, Capabilities};
use super::super::*;

pub const ARCH_NAMES: &[&str] = &[
    "Qwen2_5OmniForConditionalGeneration",
    "Qwen2_5OmniModel",
    "Qwen2_5OmniThinkerForConditionalGeneration",
];

static INFO: ArchInfo = ArchInfo::new("Qwen2_5OmniThinker", Capabilities::empty());

pub struct Qwen25OmniThinkerArchFactory;
pub static FACTORY: Qwen25OmniThinkerArchFactory = Qwen25OmniThinkerArchFactory;

impl ArchFactory for Qwen25OmniThinkerArchFactory {
    fn arch_names(&self) -> &'static [&'static str] {
        ARCH_NAMES
    }
    fn info(&self) -> &'static ArchInfo {
        &INFO
    }

    fn build(
        &self,
        cfg: &ModelConfig,
        vb: VarBuilder,
    ) -> Result<Box<dyn ModelForward>, ModelError> {
        Ok(Box::new(Qwen2_5OmniThinkerForConditionalGeneration::new(
            cfg, vb,
        )?))
    }

    fn as_any(&self) -> &dyn Any {
        self
    }
}
